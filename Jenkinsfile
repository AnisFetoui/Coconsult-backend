pipeline {
    agent any
    stages {
        stage('Git Checkout') {
            steps {
                git branch: 'principal', url: 'https://github.com/AnisFetoui/Coconsult-backend.git'       }
    }
}
}
